<%= form_with(model: table) do |form| %>
  <% if table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(table.errors.count, "error") %> prohibited this table from being saved:</h2>

      <ul>
        <% table.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :title, class: 'form-label' %>
    <%= form.text_field :title, class: 'form-control w-50' %>
  </div>

  <div class="mb-3">
    <%= form.label :file, class: 'form-label' %>
    <%= form.file_field :file, class: 'form-control w-50' %>
  </div>

  <div>
    <%= form.submit class: 'btn btn-primary'%>
  </div>
<% end %>
